import { Day } from "../../models/types"

export default {
    solvePartOne: (input: string[]): string => {
        return ''
    },
    solvePartTwo: (input: string[]): string => {
        return ''
    }
} as Day